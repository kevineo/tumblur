<%= form_for :post, url: posts_path do |f| %>
	<p>
		<%= f.label :post_title %><br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :post_body %><br>
		<%= f.text_area :body %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<%end%>		